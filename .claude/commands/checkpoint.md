整理当前工作进度并存档。**进度的唯一账本是 `AUDIT.md`「零、进度看板」——不在 CLAUDE.md 里记进度。**

1. 更新 `AUDIT.md`「零、进度看板」：这一场动过的问题编号逐条改状态、填 PR 号；新发现的问题按编号追加（编号永不复用），并在下面补一条详情条目。
2. 在 `DEV_LOG.md` 顶部（倒序，新在上）追加一段人话小结：这一场做了什么、验证了什么、留下什么坑。
3. `CLAUDE.md` 只在**契约 / 红线 / API 坑 / 协作纪律**真的变了时才动；发现其中有过时事实（stale fact）就顺手修正，但**不记进度明细**。

更新完后，最后用一两句话告诉我你记录了什么，方便我确认。
