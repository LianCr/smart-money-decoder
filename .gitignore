# API keys — 绝不提交
.env

# macOS 系统文件
.DS_Store

# Claude Code 本地配置（含项目记忆，不上传）——但两处例外：
# settings.json 是 commit gate（英文 commit + 测试必绿）的配置，是纪律的"牙齿"；
# commands/ 是 /checkpoint 等协作流程的定义。不入库就等于换台机器/换个人纪律就消失了。
# 已确认其中无任何密钥。
.claude/*
!.claude/settings.json
!.claude/commands/

# 虚拟环境
.venv/

# 新闻缓存（本地开发产物，不需要共享）
.cache/

# 诚实记分牌运行时档案（从装上往后累积，实例本地数据，不共享）
.data/

# Python 编译缓存
__pycache__/
*.pyc


# 开发过程截图（不入库）
截屏*.png
*.png

# 面试复习手册（私人文件，不入库）
INTERVIEW_PREP*.md

# 前端构建产物（Render 每次 npm run build 重生成，不入库；本地看前端需先 build）
frontend/dist/

# 简历等私人文档，防误入库
*.docx
